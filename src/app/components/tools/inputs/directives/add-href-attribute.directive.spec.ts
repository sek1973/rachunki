import { AddHrefAttributeDirective } from './add-href-attribute.directive';

describe('AddHrefAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new AddHrefAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
