import { InputPercentDirective } from './input-percent.directive';

describe('InputPercentDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPercentDirective();
    expect(directive).toBeTruthy();
  });
});
